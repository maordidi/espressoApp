var config = {

	database: {
		host:	  'localhost', 	// database host
		user: 	  'root', 		// your database username
		db: 	  'espressodb' 		// your database name
	},
	server: {
		host: '127.0.0.1',
		port: '3306'
	}
}

module.exports = config